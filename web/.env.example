# Variáveis do frontend (Vercel). Copie para .env.local no dev, ou configure na Vercel.
# Sem elas, o app roda em modo dev (sem login).
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...   # chave anon (pública) do Supabase
VITE_API_BASE=https://almoxarifado-r21-api.onrender.com   # URL do backend no Render
